const popupContent = {
    textAlign: "center",
    height: "350px",
    marginTop: "30px"
};
const popupHead = {
    fontWeight: "bold",
    fontSize: "22px"
};

const popupText = {
    fontSize: "15px",
    marginBottom: "20px"
};

const okText = {
    fontSize: "15px"
};

const popButton = {
    backgroundColor: '#4CAF50',
    border: 'none',
    color: ' white',
    padding: '3px 5px',
    textAlign: 'center',
    textDecoration: 'none',
    display: 'inline-block',
    fontSize: '12px',
    width: '80px'
}

export { popupContent, popupHead, popupText, okText, popButton };
